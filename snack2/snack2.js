// 🏆 Snack 2
// Crea una arrow function che calcola il quadrato di un numero.

const quadrato = (num) => num * num;
console.log(quadrato(10));


// Definisci una funzione chiamata quadrato che accetta un numero e restituisce il suo quadrato in una sola riga.


// Inizialmente ho pensato subito all'arrow funtion per abbreviare la scrittura, ma ho deciso di scrivere la funzione in modo tradizionale per completezza, rispettando comunque l'esercizio

function quadrato(num) { return num * num; }  
console.log(quadrato(10));